 
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-lg">
                <div class="card-body">
                    <h4 class="card-title text-center text-uppercase fw-bold">
                        <i class="bx bx-edit-alt text-warning"></i> Gérer la Catégorie
                    </h4>

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                        <div class="mb-3">
                            {{ form_label(form.nom, 'Nom de la Catégorie', {'label_attr': {'class': 'fw-bold'}}) }}  <span class="text-danger"> * </span>
                            {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
                        <span class="text-danger">  {{ form_errors(form.nom) }} </span>
                        </div>

                        <div class="d-flex justify-content-center mt-4">
                            <button class="btn btn-success me-2">
                                <i class="bx bx-save"></i> {{ button_label|default('Enregistrer') }}
                            </button>
                            <a href="{{ path('app_category_index') }}" class="btn btn-outline-secondary">
                                <i class="bx bx-arrow-back"></i> Annuler
                            </a>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
