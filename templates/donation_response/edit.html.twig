{% extends 'home/base.html.twig' %}

{% block title %}Edit Donation Response{% endblock %}

{% block body %}
<div class="container" style="margin-top: 220px;margin-bottom: 500px;">

        <h1 class="text-center mb-4">Modifier la réponse de donation</h1>

        {{ include('donation_response/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-3 d-flex justify-content-between">
            <a href="{{ path('app_donation_response_index') }}" class="btn btn-secondary">Back to list</a>
            {{ include('donation_response/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
