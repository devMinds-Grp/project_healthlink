{% extends 'home/base.html.twig' %}

{% block title %}Donation Response Details{% endblock %}

{% block body %}
<div class="container" style="margin-top: 220px;margin-bottom: 500px;">

        <h1 class="text-center mb-4">Donation Response Details</h1>

        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th class="table-dark">Description</th>
                    <td>{{ donation_response.Description }}</td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex justify-content-between mt-3">
            <a href="{{ path('app_donation_response_index') }}" class="btn btn-secondary">Retoure a la liste</a>
            <a href="{{ path('app_donation_response_edit', {'id': donation_response.id}) }}" class="btn btn-warning">Modifier</a>
            {{ include('donation_response/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
