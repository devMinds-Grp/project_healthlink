<!DOCTYPE html>
{% extends 'home/base.html.twig' %}
<title>CareResponse</title>

{% block body %}
<div class="container" style="margin-top: 220px;margin-bottom: 500px;"> 
    <h1>CareResponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ care_response.id }}</td>
            </tr>
            <tr>
                <th>Date_rep</th>
                <td>{{ care_response.dateRep ? care_response.dateRep|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Contenu_rep</th>
                <td>{{ care_response.contenuRep }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_care_response_index') }}">back to list</a>

    <a href="{{ path('app_care_response_edit', {'id': care_response.id}) }}">edit</a>

    {{ include('care_response/_delete_form.html.twig') }}
{% endblock %}
